Si consideri la componente grafica Swing

com/hotmoka/examples/swing/slider/StretchableImage.java

che visualizza un'immagine allungata orizzontalmente o verticalmente
sulla base di due coefficienti specificati tramite i metodi

setXScaleFactor(double)
setYScaleFactor(double)

Si definisca la sottoclasse SliderFrame di JFrame che crea una
finestra come nello screenshot incluso in questa stessa directory.

Lo slider deve allungare orizzontalmente o verticalmente l'immagine.
La direzione di allungamento è specificata tramite due radio button.
